@{
  Layout = "_Layout";
}

@model Crafting.Models.Item;

@using (Html.BeginForm())
{
    @Html.LabelFor(model => model.ItemName)
    @Html.TextBoxFor(model => model.ItemName, new {@required = "required"})
    <input type="submit" value="Add item" />
}